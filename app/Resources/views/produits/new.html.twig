{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creation du produit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Valider" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('produits_index') }}">Retour a la liste des produits</a>
        </li>
    </ul>
{% endblock %}
