{% extends 'base.html.twig' %}

{% block body %}
    <h1>Client</h1>

    <table class="table table-bordered table-striped table-condensed">
        <tbody>
            
            <tr>
                <th>Numero Client</th>
                <td>{{ medecin.identifiant }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medecin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ medecin.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ medecin.adresse }}</td>
            </tr>
        </tbody>
    </table>
<div class="row">
            

            <div class="span1">
            <a class="btn btn-primary" href="{{ path('medecin_edit', { 'id': medecin.id }) }}">Modifier</a>
       
         </div>
            <div class="span1">


            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-primary">
            {{ form_end(delete_form) }}

    </div>
    </div>
       
{% endblock %}
